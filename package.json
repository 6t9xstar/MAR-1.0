{
  "name": "mar-1.0",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "lint": "biome check --apply src/",
    "format": "biome format --write src/",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "api:dev": "cargo run -p api-server",
    "api:build": "cargo build -p api-server --release",
    "inference:dev": "cargo run -p inference",
    "inference:build": "cargo build -p inference --release"
  },
  "dependencies": {
    "solid-js": "^1.9.0",
    "@solidjs/router": "^0.16.3",
    "marked": "^15.0.0",
    "dompurify": "^3.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@biomejs/biome": "^1.9.0",
    "@solidjs/testing-library": "^0.8.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0",
    "vitest": "^2.1.0",
    "lightningcss": "^1.28.0",
    "@types/dompurify": "^3.2.0"
  }
}
